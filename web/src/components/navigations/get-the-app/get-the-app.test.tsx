import { render } from "@testing-library/react";
import GetTheApp from ".";

describe("GetTheApp component", () => {
  it("renders the component with correct links and images", () => {
    render(<GetTheApp />);
  });
});